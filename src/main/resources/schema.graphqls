type Query {
    allPeople: [Person!]!
    allFilms: [Film!]!
    personById(id: ID): Person
    filmById(id: ID): Film
}

type Person {
    id: ID
    name: String
    height: Int
    films: [Film!]!
}

type Film {
    id: ID
    title: String
    director: String
    people: [Person!]!
}
